<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include("../partials/_navbar.ejs") %>
    <h1 class="greeting">Hello, <%= user.username %>!</h1>
    <p class="user-level">Level: <%= user.level %></p>
    <br><br>
    <% if (!user.selectedPet) { %>
    <form action="/auth/select-pet" method="POST">
        <h3>Choose Your Starter Pet:</h3>
        <label>
            <input type="radio" name="selectedPet" value="mushroom" required>
            <img src="/assets/pets/mushroom-buddy-icon.png" alt="Mushroom Buddy">
        </label>
        <label>
            <input type="radio" name="selectedPet" value="bear">
            <img src="/assets/pets/bear-buddy-icon.png" alt="Bear Buddy">
        </label>
        <label>
            <input type="radio" name="selectedPet" value="ducky">
            <img src="/assets/pets/ducky-buddy-icon.png" alt="Ducky Buddy">
        </label>
        <button type="submit">Confirm Pet</button>
    </form>
<% } else { %>
    <img src="<%= user.selectedPet.image %>" />
    <p>🐾 Starter Pet: <strong><%= user.selectedPet.name %></strong></p>
<% } %>
    <a href="/account/dashboard" class="btn">Start</a>
    <br><br>
    <button id="howToPlayBtn" class="btn">How to Play</button>
    <div id="howToPlayInstructions" class="how-to hidden">
    <h3>🐾 How to Play Task Buddy</h3>
    <ul>
        <li>✅ Complete tasks to gain XP.</li>
        <li>⭐ Level up to unlock new pets, backgrounds, and decorations.</li>
        <li>🎨 Use your inventory to customize your pet's environment.</li>
        <li>🧸 Only one pet, background, and decoration can be active at a time.</li>
        <li>📦 New items will appear in your inventory as you level up!</li>
    </ul>
    </div>
    <br><br>
    <a href="/auth/sign-out" class="btn">Logout</a>
    <br><br>
    <script src="/script.js"></script>
    <%- include("../partials/_footer") %>
</body>
</html>